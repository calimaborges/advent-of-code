#include <stdio.h>
#include <stdbool.h>
#include <string.h>

#define MAX_INPUT 100

int main() { 
	char input[MAX_INPUT];

	// read command
	// read command modifier (if needed)
	// read start interval
	// read through keyword
	// read end interval


	while (scanf("%99s", input) != EOF) {
		printf("%s\n", input);
	}
	return 0;
}
